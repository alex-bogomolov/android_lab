<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bogomolov.alexander.androidlab">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <permission android:name="com.bogomolov.alexander.androidlab.READ_DATABASE" android:protectionLevel="normal" />
    <permission android:name="com.bogomolov.alexander.androidlab.WRITE_DATABASE" android:protectionLevel="normal" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ColorEditorActivity" />
        <activity android:name=".CalculatorActivity" />
        <activity android:name=".NotesActivity" />
        <activity android:name=".NoteEditorActivity"
                  android:parentActivityName=".NotesActivity"></activity>
        <provider
            android:authorities="com.bogomolov.alexander.androidlab.notes"
            android:name=".NotesProvider"
            android:readPermission="com.bogomolov.alexander.androidlab.READ_DATABASE"
            android:writePermission="com.bogomolov.alexander.androidlab.WRITE_DATABASE"
            android:exported="true"/>
    </application>

</manifest>